[Unit]
Description=Tech Test API
After=network.target

[Service]
Type=simple
User={{ app_user }}
WorkingDirectory={{ app_dir }}/app
ExecStart={{ app_dir }}/app/.venv/bin/uvicorn main:app --host 0.0.0.0 --port 7999
Restart=on-failure

[Install]
WantedBy=multi-user.target
