[Unit]
Description=Tech Test API
After=network.target

[Service]
Type=simple
User=ec2-user
WorkingDirectory={{ app_dir }}
ExecStart={{ app_dir }}/.venv/bin/uvicorn main:app --host 0.0.0.0 --port 7999
Restart=on-failure

[Install]
WantedBy=multi-user.target
